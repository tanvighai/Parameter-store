pipeline {
    agent { label 'Ansible-workstation'}
    options {
       ansiColor('xterm')
    }

    }
    stages {
        stage('Terraform Apply') {
            steps {
                sh 'make'

                }
              }
        }
        }
